# TIMELABS_SEMANTIC_NEIGHBOURS_EXECUTIVE

## 100/10 — SHARE ACROSS ALL TIMELABS
1. **Do not reinvent policy/evidence substrates:** combine policy-as-code (OPA/Cedar class) + append-only source-record + provenance receipts (in-toto/TUF/transparency-log primitives).
2. **Adopt one shared doctrine:** **read-only → replay → bounded mutation**, with explicit UNKNOWN/ERROR fail-closed semantics.
3. **Preserve “consensus is evidence, not truth” as constitutional invariant** across Omnia, Tribunal, Blueshoes, HME, World2.
4. **Unify vocabulary with Chinese deltas** (标识/定位/命名 decomposition; 算力网络; 意图驱动 lifecycle) to avoid hidden category errors.
5. **Use OpenBSD-native mechanism palette first** for MBSD (PF/anchors/tables/divert/CARP/pfsync) before importing foreign dataplane complexity.

## 10/10 — MUST NOT BE LEFT UNATTENDED
- Cloud-authoritative drift risk (explicitly prevented in Blueshoes constitution; must remain enforced in all cells).
- Majority-vote trap in multi-agent governance (must remain subordinate to reproducible evidence tiers).
- Replay-vs-truth confusion (deterministic replay proves consistency, not correctness).
- Naming/identity/location conflation in network architecture decisions.
- Missing MBSD repository-level evidence leaves kernel/userspace boundary classification partially unresolved.

## ALREADY INVENTED ELSEWHERE
- Policy decision/enforcement separation (PDP/PEP families).
- Append-only event sourcing with rebuildable projections.
- Signed provenance/attestation/transparency logs.
- Atomic mutation + rollback transaction loops.
- Multipath/mobility/NAT traversal protocol families.

## WRAP, DON’T REWRITE
- OPA/Cedar (policy provider lane)
- WireGuard/ICE/MASQUE (connectivity substrate lane)
- in-toto/Sigstore/TUF (evidence provider lane)
- libp2p discovery stack (rendezvous/discovery provider lane)

## POSSIBLY GENUINELY NEW
- Projection-honesty contract shape (explicit preserve/drop/aggregation + deterministic replay invariant) if carried through implementation and adoption.
- Cross-cell constitutional composition linking Omnia constraints, Tribunal disagreement contracts, and Blueshoes edge sovereignty in one typed runtime contract.

## CHINESE ONTOLOGY DELTAS
- **标识网络** introduces explicit separation between human naming, cryptographic identity, and locator resolution.
- **算力网络** adds compute-routing as first-class path-selection factor.
- **意图驱动网络** frames intent compilation as lifecycle pipeline, not static ACL editing.
- **具身智能 + 世界模型** in Chinese usage is wider than RL latent-world-model framing and better aligns with World2/HME embodiment goals.

## NEXT EXCAVATIONS
1. GNUnet/GNS deep dismantling against Timelabs naming/identity sovereignty goals.
2. SCION vs Blueshoes/MBSD deployability map with explicit incremental path.
3. Formal argumentation + belief-revision algorithm selection for Tribunal disagreement closure.
4. MBSD repository-level kernel/userspace capability inventory and proof contracts.
5. Chinese primary-paper extraction pack (CAICT + top university labs) for high-confidence ontology splits.
